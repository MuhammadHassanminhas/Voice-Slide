# Copilot Instructions

## Project Guidelines
- User corrected that \b does not trigger between a letter and a digit since both are \w characters. When documenting regex safety traces, remember that \b only fires at \w↔\W transitions or string boundaries.